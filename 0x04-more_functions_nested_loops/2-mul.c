#include "main.h"

/**
 * mul - Multiplies two integers
 * @x: The first number to multiply
 * @y: The secong number to multiply first number
 * Return: The result of multiplication between x param and y param
 */

int mul(int x, int y)

{
	return (x * y);
}
